# 目录结构

第一版实际结构（截至第一版完成）。

```text
personal_navigation/
├── index.html              # 首页，导航卡片入口
├── status.html             # 服务状态页
├── _headers                # Cloudflare Pages 响应头：CSP / HSTS / 缓存策略
├── .gitignore
├── readme.md               # 项目说明
├── css/
│   └── style.css           # 全部样式（含深浅色变量、响应式）
├── js/
│   └── theme.js            # 主题切换（自动 / 浅色 / 深色）与头像切换
├── functions/
│   └── api/
│       └── status.js       # Pages Function：/api/status，代理 UptimeRobot API
├── assets/
│   ├── headimg/            # 头像（浅色 blue / 深色 dark 等）
│   └── websiteimg/         # 站点与服务图标
└── docs/
    ├── 个人导航页——项目构想与开发计划.md
    └── 目录结构.md
```

## 说明

- `functions/` 是 Cloudflare Pages Functions 的约定目录，文件路径直接映射为接口
  路径：`functions/api/status.js` → `/api/status`。该目录不会作为静态资源发布。
- `_headers` 是 Cloudflare Pages 的约定文件，用于配置响应头，无需构建步骤。
- 项目没有 `package.json`，也没有任何构建产物，仓库内容即为部署内容。
- 已弃用的 PHP 版状态页（`index.php`）及其缓存文件已从版本控制中移除，并在
  `.gitignore` 中忽略。